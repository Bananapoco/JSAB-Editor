/** @type {import('next').NextConfig} */
const nextConfig = {
    // Removed 'output: export' to enable API routes
    // API routes require a server and don't work with static export
};

export default nextConfig;
